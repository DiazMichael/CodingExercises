using System;
using System.Linq;

namespace StringSorting
{/// <summary>
/// Create a function that takes a string consisting of lowercase letters, 
/// uppercase letters and numbers and returns the string sorted in the same way as the examples below.
/// </summary>
    class Program
    {
        static void Main(string[] args)
        {
            var str = "eA2a1E";
            Console.WriteLine(sorting(str));
        }

        private static string sorting(string str)
        {
            return 
        }
    }
}
