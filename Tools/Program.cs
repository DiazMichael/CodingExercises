using System;

namespace Tools
{
    class Program
    {
        static void Main(string[] args)
        {
            var integer = 0;
            var sample = ""; 
            var array = new Array[10];
            int length = array.Length; /// array.Length returns the int value of the length of the array
            var x = integer.ToString(); /// Converts into a string the value
            array.
        }
    }
}
